"""PyJHora wrappers — one module per endpoint.

Each module exposes a single ``compute(...)`` returning plain JSON-ready dicts. Nothing here
reads or writes state, and nothing here interprets a result.
"""
